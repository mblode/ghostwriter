# Ghostwriter

Retired on 21 September 2026. The skill lives in [agent-skills](https://github.com/mblode/agent-skills) as `skills/ghostwriter`:

```bash
npx skills add mblode/agent-skills -g --agent codex claude-code -y
```

Your profiles under `GHOSTWRITER_HOME` (default `~/.config/ghostwriter`) are unchanged and are read from there.
